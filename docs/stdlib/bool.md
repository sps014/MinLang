# bool

`bool` represents a boolean value (`true` or `false`). These methods are available on any `bool` value. All are auto-imported — no import needed.

## to_int

Returns `1` for `true` and `0` for `false`.

```ts
println(true.to_int());    // 1
println(false.to_int());   // 0
```
