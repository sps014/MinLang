async fun work(id: int): string {
    await sleep(10);
    return "u";
}

async fun main(): void {
    let a = work(1);
    let b = work(2);
    let both = await all([a, b]);
    println("all = " + both[0] + ", " + both[1]);
}
