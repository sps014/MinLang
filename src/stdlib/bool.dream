// The `bool` primitive as an extensible class. A `bool` is an `i32` (0 or 1) at runtime.
// Auto-imported into every program.

extend bool {
    // 1 for true, 0 for false.
    fun to_int(): int {
        if (this == true) {
            return 1;
        }
        return 0;
    }
}
