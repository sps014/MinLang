async fun delayedDouble(n: int): int {
    await sleep(100);
    return n * 2;
}

async fun main(): void {
    System.println("start");
    let a = await delayedDouble(10);
    let b = await delayedDouble(a);
    System.println("result = " + to_string(b));
    System.println("done");
}
