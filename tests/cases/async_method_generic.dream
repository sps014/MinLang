class Box<T> {
    value: T;

    async fun get(): T {
        await sleep(10);
        return this.value;
    }
}

async fun main(): void {
    let b = Box<int>(7);
    let v = await b.get();
    System.println(to_string(v));
}
