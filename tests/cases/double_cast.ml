fun main(): void {
    let i: int = 7;
    let d: double = (double)i;
    println(d);

    let f: float = (float)d;
    println(f);

    let back: int = (int)d;
    println(back);

    let g: double = (double)f;
    println(g);
}
