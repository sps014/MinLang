enum Direction {
    North,
    East = 10,
    South,
    West,
}

fun main() {
    let d = Direction.South;
    System.println(d.name());
    System.println(Direction.North.name());
    System.println(Direction.West.name());
    System.print(Direction.East.name());
    System.println("!");
}
