fun fib(n: int): int {
    if (n <= 1 ) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

fun add3(a: int, b: int, c: int): int {
    return a + b + c;
}

fun main(): void {
    System.println(fib(5));
    System.println(fib(7));
    System.println(add3(10, 20, 30));
}
