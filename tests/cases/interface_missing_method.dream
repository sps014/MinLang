// A class that declares it implements an interface but omits one of its methods must be rejected.

interface Animal {
    fun speak(): string;
    fun legs(): int;
}

class Cat : Animal {
    public fun speak(): string { return "meow"; }
    // Missing `legs(): int` — should be a compile error.
}

fun main(): void {
    let c = Cat();
}
