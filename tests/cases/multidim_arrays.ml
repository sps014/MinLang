fun main() {
    let grid: int[][] = [[1, 2, 3], [4, 5, 6]];
    println(grid.len());
    println(grid[0].len());
    println(grid[1][2]);

    let words: string[][] = [["hello", "world"], ["foo"]];
    println(words[0][1]);
    println(words[1][0]);
}
