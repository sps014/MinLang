// Two overloads match (int,int) equally well via `object` widening -> ambiguous call error.
fun g(a: object, b: int): int { return 1; }
fun g(a: int, b: object): int { return 2; }

fun main(): void {
    println(g(5, 5));
}
