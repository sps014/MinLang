// Overloaded free functions resolve by argument type.
fun kind(x: int): int { return 1; }
fun kind(x: string): int { return 2; }

fun main(): void {
    println(kind(42));     // 1  (int overload)
    println(kind("hi"));   // 2  (string overload)
}
