// A class method that shadows an object-protocol method must be marked `@override`.
class Foo {
    x: int;

    public fun to_string(): string {
        return "foo";
    }
}

fun main() {
    let f = Foo(1);
    System.print(f);
}
