class Box {
    _secret: int;

    fun reveal(): int {
        return this._secret;
    }
}

fun main(): void {
    let b = Box(42);
    // Accessing a private (`_`-prefixed) field from outside the type is a compile error.
    System.println(b._secret);
}
