fun side(): bool {
    System.println(99);
    return true;
}

fun main(): void {
    let f: bool = false;
    if (f && side()) {
        System.println(1);
    }

    let t: bool = true;
    if (t || side()) {
        System.println(2);
    }

    System.println(0);
}
