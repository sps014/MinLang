fun main(): void {
    let x: int = 5;
    let y: int = x > 3 ? 100 : 200;
    System.println(y);

    let s: string = x > 10 ? "big" : "small";
    System.print(s);
    System.print("\n");

    let z: int = x > 10 ? 1 : x > 3 ? 2 : 3;
    System.println(z);
}
