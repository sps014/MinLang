{
  "name": "dream-web-lsp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "build:wasm": "wasm-pack build ../dream-analyzer --target web --out-dir ../web/src/pkg --out-name dream_analyzer"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  },
  "dependencies": {
    "monaco-editor": "^0.52.0"
  }
}
